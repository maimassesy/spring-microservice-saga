spring:
  cloud:
    gateway:
      routes:
        - id: test-route
          uri: http://localhost:12345
          predicates:
            - Path=/test/**
  data:
    redis:
      host: localhost
      port: 6379

app:
  jwt:
    secret: mySecretKey1234567890123456789012345678901234
